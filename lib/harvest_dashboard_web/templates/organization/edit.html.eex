<h2>Edit Organization</h2>

<%= render "form.html", Map.put(assigns, :action, organization_path(@conn, :update, @organization)) %>

<span><%= link "Back", to: organization_path(@conn, :index) %></span>
